# 项目总结

按迭代记录「做了什么 / 踩坑 / 下次注意」。方案与操作细节以同目录其他文档为准，本文件只留经验教训。

---

## 2026-09-20 · S9 服务器部署上线 + S10 收尾

线上地址：`http://47.120.34.207:8080`（前端 Nginx 8080 → 后端容器 8010 → MySQL 容器只在 Docker 内网）

### 做了什么

- Docker Compose 起 MySQL 8 + 后端，前端在本机 Vite 构建后以静态站点交付
- 宝塔建站 + Nginx 反向代理 `/api/`，与既有项目 miaocun 共存（端口错开 8010/8080）
- 初始化管理员账号，打通全链路 E2E：上传培训资料 → 发布考核 → 员工答题 → 自动判分 → 复盘报告 → 管理端成绩报表
- 修复成绩报表「按考核筛选」下拉框显示原始 ID 的缺陷
- 新增 `samples/信息安全与数据合规培训手册.md` 作为可复现的 E2E 演示素材

### 踩坑

| 坑 | 现象 | 真因 |
| --- | --- | --- |
| 宝塔「域名」框三条校验 | `IP:端口` 报端口不合法、纯数字报格式错误、同 IP 报已存在 | 该框只做站点标识，需填占位子域名再去配置文件补 `server_name` |
| 手改 Nginx 粘贴位置 | `nginx: [emerg] "location" directive is not allowed here` | 宝塔生成的 `error_log` 后已有闭合 `server` 的右花括号，粘贴点在其外 |
| 逐个文件上传 dist | 整页白屏，接口正常 | 宝塔上传不带 `assets/` 子目录，`index.html` 引用的 JS 404 |
| 误判「线上代码过期」 | `ImportError: get_password_hash`、`async_session_maker` | 密码哈希在 `app/core/password.py`，DB 层是 aiomysql 原生池而非 SQLAlchemy；我按常见 FastAPI 套路猜了实现，还顺手改了 `security.py`、多跑了一次镜像重建 |
| 登录 401 排查绕远路 | 健康检查正常、哈希看着也正常 | 项目里那个管理员账号的哈希与本项目验证函数不配套；正解是删号重建，不是查 MySQL 密码 |
| 下拉框显示 `asmt_xxx` | 数据明明加载成功，只有下拉框不对 | 后端所有分页接口 `page_size` 上限 50，前端传 100 → 422 → 被 `.catch(() => {})` 静默吞掉 |
| `.env` 位置误判 | `docker exec ... cat /app/.env` 报不存在，一度以为没配 | `env_file: .env` 相对 compose 文件解析，生效路径是仓库根的 `.env`，容器内本就不该有 |

### 下次注意

1. **断言"代码缺失/镜像过期"之前，先在本地仓库 grep 找真实实现。** 一个 `ImportError` 不等于功能没写，更可能是自己 import 错了模块。这次因此白跑一次镜像构建，属于纯浪费。
2. **新增 import 前先查 `requirements.txt`。** 我往 `security.py` 加了 `passlib` 而项目只装了 `bcrypt`，若不是及时发现会直接让容器起不来。
3. **前端 `.catch(() => {})` 是排查黑洞。** 静默吞掉的 422 让一个改数字就能修的问题找了十几分钟；本地联调阶段至少临时打印一次错误。
4. **分页参数以后端 Query 校验上限为准**，本项目统一 `le=50`。要一次拉全量就翻页，别赌大数。
5. **静态站点发布一律整包压缩上传**，并在解压后核对 `assets/` 里的 hash 与 `index.html` 引用一致；验收前 Ctrl+F5。
6. **线上初始化管理员不必重建镜像**，`docker exec` 直接调容器内模块即可；把命令固化进部署手册第 10 节。
7. **改 `.dockerignore` 要基于原文件增量调整**，不要整篇重写。这次把原作者的注释和 `.env.*`、`!.env.example` 等规则一并抹掉了。
8. 演示环境仍是 HTTP + IP，未走 HTTPS/备案；对外演示前如需证书，按手册第 9 节做 DNS 验证签发的免费证书。

### 已知小瑕疵（未修）

- 管理员昵称显示为「学习者」：直接建号时未设 `nickname`，一条 `UPDATE users SET nickname='系统管理员' WHERE username='admin'` 可修
- 员工列表时间显示带 `T`（`2026-09-20T18:17:52`），成绩报表是空格分隔，两处后端返回格式不一致
